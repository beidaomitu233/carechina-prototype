# CareChina 项目协作规范

## 高保真原型文案规则（强制）

本项目是面向客户评估的高保真产品原型。页面应表现为真实可用的产品，不得写成产品逻辑说明书、设计汇报稿或原型使用指南。

### 1. 标题只说明“这里是什么”

- 页面标题、模块标题和卡片标题必须直接说明对应的业务内容。
- 不要在标题中解释模块为什么这样设计、用户应该按什么顺序理解，或页面将如何响应。
- 标题下方若已由表单、按钮、标签、数字或图像表达清楚，不再补充重复说明。
- 优先使用“医院目录”“六步医疗协同流程”“咨询与费用规划”等直接名称。

### 2. 禁止出现原型和评审元信息

面向用户的页面中禁止出现下列类型的措辞：

- “这是原型”“静态原型”“原型不会发送或保存信息”；
- “用于产品与设计评审”“正式产品将会……”；
- “当前业务调研重点覆盖……”；
- “本页面用于……”“页面不会带你离开当前页面”；
- 图片授权、设计取样、开发阶段、数据配置状态等内部说明。

交互成功状态必须使用真实产品语言，例如“咨询需求已提交”“协调员将确认下一步”，不得使用“预览完成”“本地完成预览”“生产环境会……”。

### 3. 不要在操作前强迫用户阅读规则

- 能通过控件名称、占位符、选项和按钮表达的内容，不再写成段落。
- 操作提示应出现在需要它的时刻，例如空状态、校验错误、提交结果或医院详情中。
- 标签页和筛选器只保留名称；删除“推荐从这里开始”“选择后列表会原位更新”等页面操作解释。
- CTA 标题与按钮已经表达动作时，不再增加一段重复描述。

### 4. 保留真正影响患者决策的信息

以下内容不属于应删除的“解释性文字”，应在必要位置简洁保留：

- 急救边界和高风险症状提示；
- 医院是否接诊、预约名额和服务可用性需要确认；
- 费用区间不是医院正式报价以及可能变化的因素；
- 隐私授权、病历提交时点和安全传输要求；
- 诊断、处方、疗效和临床决策必须由合格医疗人员确认；
- 数据来源、医院官网入口及会影响选择的业务事实。

安全提示应以患者行动或服务边界为主语，不使用“本页内容”“本原型”等元叙述。

### 5. 中英文文案要求

- 中英文都应按各自语言习惯撰写，不做生硬直译。
- 中文避免连续使用大段黑粗体、口号和规则解释。
- 英文避免冗长复合句、营销堆词和所有格造成的异常断行。
- 两种语言表达的业务含义、服务边界和交互状态必须一致。

## 修改页面后的验收清单

每次修改 HTML 或可见文案后必须检查：

1. 全站搜索 `prototype`、`原型`、`产品与设计评审`、`业务调研` 等元信息，确认用户页面中没有残留。
2. 检查标题下方是否仍有与标题、卡片或控件重复的说明段落。
3. 检查空状态、提交成功、医院详情和客服回复是否使用真实产品语言。
4. 保证删除文案后没有异常空白、孤立标签或布局断层。
5. 验证英文、中文、桌面端和手机端均无横向溢出，核心交互仍然可用。
